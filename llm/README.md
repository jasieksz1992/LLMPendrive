Place the portable llama.cpp server executable in this directory.

Required executable:

```text
llm\llama-server.exe
```

To download it automatically on Windows, run from the repository root:

```bat
scripts\download-assets.bat -SkipModel
```
